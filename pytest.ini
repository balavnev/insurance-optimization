[pytest]
markers =
    slow: full-iteration-budget solution-quality checks against the real reference solutions (opt-in: pytest -m slow)
    llm_integration: needs a real LLM_BASE_URL-configured endpoint (opt-in: pytest -m llm_integration); skips itself when unset
